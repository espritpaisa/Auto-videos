name: Automatizar videos

on:
  push:
    paths:
      - 'videos/**'

jobs:
  prueba:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repo
        uses: actions/checkout@v3

      - name: Ejecutar prueba
        run: echo "Funcionando correctamente 🚀"
